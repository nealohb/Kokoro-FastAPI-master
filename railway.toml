[build]
builder = "nixpacks"
buildCommand = "pip install -e .[cpu]"

[deploy]
startCommand = "uvicorn api.src.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 100

[env]
PYTHON_VERSION = "3.10" 